---

redeploy_schemes_supported: ['_scheme_addallnew_rmdisk_rollback', '_scheme_addnewvm_rmdisk_rollback', '_scheme_rmvm_rmdisk_only', '_scheme_rmvm_keepdisk_rollback', '_scheme_noredeploy_scale_in_only']

#omit_singleton_hosttype_from_hostname: true             # If we don't want the hosttype in the hostname (only valid if there's only one hosttype), e.g. 'k8s-dev-etcd-euw1-a0' vs 'k8s-dev-euw1-a0'.  Default false (ansible_collections/clusterverse/clusterverse/roles/_dependencies/defaults/main.yml)
#omit_singleton_region_from_hostname: true               # If we don't want the region in the hostname (only valid if there's only one region), e.g. 'k8s-dev-etcd-euw1-a0' vs 'k8s-dev-etcd-a0'.  Default false (ansible_collections/clusterverse/clusterverse/roles/_dependencies/defaults/main.yml)

update_os: true                                         # A variable passed to the cloud-init user-data script (or the startup-script for GCP), to indicate that it can run any user-defined run_cmd (apt-get -y dist-upgrade) [default: false].

app_name: "k8s"                                         # The name of the application cluster (e.g. 'couchbase', 'nginx'); becomes part of cluster_name.  Provided is a default to ensure no accidental overwriting.

cluster_name: "{{app_name}}-{{buildenv}}"               # Identifies the cluster within the cloud environment; becomes part of the hostname

cluster_vars:
  custom_tagslabels:
    environment: "{{buildenv}}"
    owner: "{{ lookup('pipe', 'whoami') | lower }}"
