---

merge_dict_vars_list:
  - "{{playbook_dir}}/cluster_defs/"
  - "{{playbook_dir}}/cluster_defs/{{ cloud_type }}/"
  - "{{playbook_dir}}/cluster_defs/{{ cloud_type }}/{{ clusterid | default() }}/"
  - "{{playbook_dir}}/cluster_defs/{{ cloud_type }}/{{ clusterid | default() }}/{{ buildenv }}/"
  - "{{playbook_dir}}/cluster_defs/{{ cloud_type }}/{{ clusterid | default() }}/{{ buildenv }}/_ssl/ca.yml"   #Do NOT include the other certs here, they are cluster_suffix specific
  - "{{playbook_dir}}/cluster_defs/{{ cloud_type }}/{{ buildenv }}/"
